@AGENTS.md

Maintainer notes live in `AGENTS.md`; this file exists only because Claude
Code reads `CLAUDE.md` and not `AGENTS.md`. The `@AGENTS.md` import is the
form Claude Code's own documentation recommends over a symlink, because a
symlink needs Administrator privileges or Developer Mode on Windows.
